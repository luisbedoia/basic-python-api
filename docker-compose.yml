# version: '3.8'

# services:
#  web:
#   build:
#    context: .
#   ports:
#   - 5000:5000
#   volumes:
#   - ./:/app

#  mysqldb:
#   image: mysql
#   ports:
#   - 3306:3306
#   environment:
#   - MYSQL_ROOT_PASSWORD=p@ssw0rd1
#   volumes:
#   - mysql:/var/lib/mysql
#   - mysql_config:/etc/mysql

# volumes:
#   mysql:
#   mysql_config:

version: '3.8'

services:
 web:
  build:
   context: .
  ports:
  - 8001:8000
  volumes:
  - ./:/app

 mysqldb:
  image: postgres:latest
  # ports:
  # - 5432:5432
  environment:
  - POSTGRES_USER=arias
  - POSTGRES_PASSWORD=saira
  volumes:
  - postgresql:/var/lib/postgresql/data

volumes:
  postgresql: