version: "3.8"

services:
  db:
    image: postgres:latest
    ports:
    - 5432:5432
    environment:
      - POSTGRES_USER=bob
      - POSTGRES_PASSWORD=bob
    volumes:
        - ./init.sql:/docker-entrypoint-initdb.d/init.sql
  
  api:
    build:
      context: .
    ports:
      - 8002:8000
    volumes:
      - ./:/app
    links:
      - "db:database"
